<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="FinanKey.View.DetalleCuentaPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    Title="paginaDetalleCuenta"
    BackgroundColor="{StaticResource FondoPrincipal}">
    <!--  Cambio de color de la barra  -->
    <ContentPage.Behaviors>
        <toolkit:StatusBarBehavior StatusBarColor="{StaticResource FondoPrincipal}" StatusBarStyle="DarkContent" />
    </ContentPage.Behaviors>

    <VerticalStackLayout Padding="30">
        <Label Text="Esta es la ventana de Detalles" />
        <Border
            BackgroundColor="{StaticResource ColorAcento}"
            Stroke="{StaticResource ColorAcento}"
            StrokeShape="RoundRectangle 10">
            <VerticalStackLayout>
                <Label Text="Cuenta Principal" />
                <HorizontalStackLayout>
                    <Label Text="Banco Sol" />
                    <Label Text="Ahorro" />
                </HorizontalStackLayout>
                <Label Text="5250,75 €" />
            </VerticalStackLayout>
        </Border>
    </VerticalStackLayout>
</ContentPage>